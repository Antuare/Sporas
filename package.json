{
  "name": "bioma-evolution-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "pnpm install",
    "dev:web": "pnpm --filter @bioma/web-renderer dev"
  }
}
